catalog_ordering_enabled - For Enabale and disable Catalogue things



Claude (prod default) also uses the catalogue menu_text and is told "Do not invent dishes, numbers, or prices" — so the LLM layer is bounded too. Let me run the suite and add a test for the describer fix:



A) Keep it (recommended) — while the photo is still processing, the customer can't tap it but can still type it to order. Nothing is lost; you just don't have the pretty card yet.

B) Fully hold — "in review" means completely unavailable: no card and typing it replies "we don't have that yet," until Meta finishes. Stricter, matches "don't link until done" literally.

Which do you want? If B, I'll add is_sendable to the typed-order gate so review blocks ordering entirely until it goes live.