{
  "name": "full-pos-desktop",
  "private": true,
  "version": "0.1.0",
  "main": "dist/main/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "npm run build && electron dist/main/main.js",
    "test": "vitest run",
    "dist": "npm run build && electron-builder --win",
    "postinstall": "echo 'desktop/: run npm run build before npm start/dist; npm run dist targets Windows (nsis) explicitly via electron-builder --win regardless of host OS, per electron-builder.yml at the project root'"
  },
  "devDependencies": {
    "electron": "^31.0.0",
    "electron-builder": "^24.13.3",
    "typescript": "^5.5.4",
    "vitest": "^2.1.1"
  }
}
