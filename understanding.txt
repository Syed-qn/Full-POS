# understanding.txt — running log of code changes + system understanding
# Format: - [YYYY-MM-DD HH:MM] <change> — <why/impact>

- [2026-06-06 10:50 +04] PHASE 3 FINAL GATE PASSED (Tasks 11/12/14/15). T11: 4 ordering-service round-trip tests (draft increments order_number, add_item recalcs total, finalize sets sla_confirmed_at/sla_deadline=+40min, get_or_create_customer idempotent) → tests/ordering/test_service.py (commit b0c1c8c). T12: 2 MockProvider pipeline integration tests (greeting→handle_inbound→outbox→MockProvider.send delivers menu; item-collection direct-match enqueues confirmation) → tests/conversation/test_engine_pipeline.py (commit 5b602b3). T14: 2 simulator E2E smoke tests (greeting returns menu; order dish→confirmation) → tests/test_simulator_ordering.py (commit e733995). WIRING FIX (production bug surfaced by T14): apps/simulator/router.py opened a FRESH async_session_factory connection for outbox delivery — invisible to the request's committed-to-savepoint rows under test transaction isolation (first test to actually ASSERT delivery; Phase-2 simulator tests only checked status-ok). Fixed: deliver via async_sessionmaker bound to session.bind w/ join_transaction_mode=create_savepoint (reuses request connection; correct for both engine-bind prod + connection-bind tests). T15 DEBT PAID: converted tests/ordering/conftest.py autouse restaurant(id=1) hardcoded-PK fixture → dynamic non-autouse `restaurant` fixture (zombie-incident footgun); rewired ALL tests/ordering/ (test_service/fsm/matching/cancellation/modification/status_reply) to take the fixture + use restaurant.id, and derived the T9 router token from the fixture's real id (_token_for(restaurant.id)) instead of create_access_token(restaurant_id=1). GATE RESULTS: full pytest 157 passed (was 149 + 8 new), ruff src apps tests CLEAN; security (tests/webhook + test_cloud_provider) 9 passed; LIVE E2E on uvicorn :8056 — /health 200 (5ms), signup 201, full simulator order flow greeting→menu(110/201)→order 110→item-added→done→address-prompt all roundtripped via /simulator/messages; security live: webhook handshake 403 on missing/wrong verify_token; PERF: simulator send max 268ms, webhook POST 2ms (all <1s). Single alembic head 4ed3145276fb (no migrations). NOT touched: frontend/, docs/ (other agents), other agents' uncommitted tree changes.
- [2026-06-06 10:40 +04] P3-T13 DONE (TDD): returning-customer stored-address offer. Extended _handle_address_capture in engine.py: on entry, if NOT conv.state.address_offer_made AND get_last_address(customer) returns a confirmed address → set flag + send BUTTONS ("Use saved address"/"New address") with room+building label, return (offers BEFORE asking for pin). Button handler: use_saved_address → _finalize_with_stored_address (new helper: attaches stored.id to draft order, haversine distance + calculate_fee from stored lat/lng, UndeliverableError→ask new location, sets last_used_at, →order_confirmation + summary); new_address → re-prompt share-location. Adapted plan tests to dynamic `restaurant` fixture + _seed_menu (NOT hardcoded id=1 per project lesson); fixed self-inflicted dish_id=1 hardcode → look up seeded Dish (FK sequence not reset across tests). 2 new tests pass.
- [2026-06-06 10:40 +04] P3-T16 DONE (TDD, gap-check vs T6): dish-description + ambiguous disambiguation. GAP found: plan test mocks app.conversation.engine.find_dish_matches but T6 imported it locally inside _handle_collecting_items (not mockable) → HOISTED `from app.ordering.matching import MatchConfidence, find_dish_matches` to engine.py module top, removed local import. T6's existing AMBIGUOUS reply already lists dish numbers + says "Did you mean" (contains "mean") and describer already strips price → no reply-format change needed. 2 new tests (what_is no-price, ambiguous lists 110/111) pass. T13+T16 share engine.py+test file → single commit 2be0bb3. FULL SUITE 149 passed, ruff src apps tests clean. Single alembic head still 4ed3145276fb (no migrations this batch). NOT touched: alembic, conftest, frontend, geo (parallel agents).
- [2026-06-06 10:40] PHASE 6 PLAN WRITTEN: docs/superpowers/plans/2026-06-06-phase-6-predictions-marketing.md (1167 lines, 20 TDD tasks). Two bounded contexts. PREDICTIONS (Tasks 1-8): pure-numpy RollingAverageModel (weekday×hour×dish rolling mean + cold-start→dish global mean) behind ForecastModel Protocol (lightgbm/sklearn/prophet swap-in later, NO heavy deps now — numpy only; HARD override of spec §4.6 LightGBM mention); features.py pure-fn observations/trailing-demand; accuracy.py MAPE; LLM ForecastAdjuster (fake rule-based + claude) → parsed_effect DSL applied via deterministic apply_overrides; service run_forecast persists prediction_runs (order_count/revenue/dish_demand/avg_distance); prep_ahead_suggestions→manager WhatsApp; router GET /api/v1/predictions/runs|latest|prep-ahead + POST /overrides; nightly_forecast 02:00 + nightly_backfill_accuracy 01:30 (ml queue). Tables prediction_runs/model_registry/manager_overrides. MARKETING (Tasks 9-17): tables wa_templates/campaigns/segments/marketing_sends/opt_outs/recurring_message_state; naming.py datestamped (^[a-z0-9_]+$, 30-day reuse blackout); compliance.py pure linter (body≤1024/footer≤60/header≤60/≤2 URL btn/no emoji-footer/no shortened-URL/sequential-vars/mandatory-opt-out); window.py UAE 09:00-18:00 Asia/Dubai; throttle.py per-user 2/24h cap (+131049 soft-suppress); optout.py STOP keyword; segments.py VALIDATED DSL (no eval/raw SQL, allowlist fields) + LLM SegmentCompiler + preview-count; TemplatePort (MockTemplateProvider auto-approve for tests + MetaTemplateProvider Graph adapter, dry-run-GUARDED so never instantiated in tests); service run_campaign_send = compliant gate (opt-out→window→cap order, per-(campaign,customer) idempotent, suppressed_* statuses recorded for analytics, Phase-4 coupon integration, conversion attribution); router /api/v1/marketing segments/preview/templates/submit/campaigns/stats; workers scheduled_campaign_tick */5 (window-gated), poll_template_statuses */2, recurring_promo_tick hourly, cleanup_ephemeral_templates 23:30 (marketing queue). Task 18 wires inbound STOP into conversation/engine.py (early guard, any state, immediate). Task 19 celery_app.py ml+marketing queues+beat. Task 20 full-suite+smoke+E2E dry-run compliant-send gate. DEFERRED: real LightGBM+weekly retrain, full automations DSL, Meta resumable image upload, TDRA DNCR integration, Ramadan/holiday/weather feature cols (passthrough=0). Config adds: forecast_provider, marketing_template_provider, marketing_window_start/end_hour, marketing_per_user_daily_cap, marketing_send_dry_run(=True default — never hits Meta unless disabled), wa_business_account_id. Every task TDD failing-test-first, real code no stubs, exact paths, self-contained. NOT committed (controller commits). DOCS-ONLY (no code touched — Phase 3 wave 4 + Phase 5 scaffold agents running concurrently).
- [2026-06-06 10:35 +04] P3-T8 DONE (TDD): 'where is my order' status reply. Added _handle_status_query() to src/app/conversation/engine.py — resolves Customer by (restaurant_id,phone); finds most-recent non-terminal Order (excludes delivered/cancelled/undeliverable/resold/written_off, order_by created_at desc); per-status human message dict; appends "~N minutes" remaining from sla_deadline for confirmed/preparing/ready when 0<remaining<=40; polite fallbacks for no-customer / no-active-order. Intent intercept in handle_inbound (after manual_takeover, before state routing): TEXT → get_intent_classifier().classify() == "status" → _handle_status_query + return (FakeIntentClassifier rule {where,status,order}→status). tests/ordering/test_status_reply.py 3 pass. Commit 4dbd919. ruff clean. INCIDENT: my engine.py edits were silently reverted once mid-run by concurrent agent's git stash/checkout interference (same footgun other agents hit) — re-applied + committed immediately to lock; learned NOT to run git stash/checkout while parallel agents share the worktree.
- 2026-06-06 10:30 +04 — P4-T1 DONE (TDD, running PARALLEL to P3 Wave 4): geo port. Added src/app/geo/{port,fake,google_maps,factory}.py — GeoPort Protocol (is_estimate flag + distance_km + eta_minutes(buffer)); FakeGeoProvider (haversine delegate + static 25km/h, ceil min 1, is_estimate=True); GoogleMapsGeoProvider STUB (graceful haversine fallback on any Maps failure, is_estimate flips True, _maps_distance raises NotImplementedError til prod key); get_geo_provider() factory @lru_cache (geo_provider env: fake default | google_maps requires key else ValueError; unknown→ValueError) — follows whatsapp/llm factory pattern. config.py +geo_provider/google_maps_api_key, .env.example +APP_GEO_PROVIDER/APP_GOOGLE_MAPS_API_KEY. tests/geo/test_geo_port.py: 5 pure-python tests pass (no DB — did NOT run full suite, Wave 4 owns test DB). ruff clean. NOTE: config.py edits twice reverted by a concurrent writer mid-run (re-applied + verified on disk). NOT touched: alembic, conversation/engine, ordering/service, conftest, frontend.
- [2026-06-06 10:30 +04] P3-T7 DONE (TDD): order modification + cancellation/resale. Appended to src/app/ordering/service.py: modify_order(order, new_items, actor) — blocks if status in _NON_MODIFIABLE_STATUSES (ready/assigned/picked_up/arriving/delivered/cancelled/undeliverable/on_resale/resold/written_off) raising ValueError "modification ... not allowed"; deletes all OrderItems, re-adds from new_items, recalcs subtotal/total, RESTARTS SLA clock (sla_confirmed_at/sla_deadline=+40min/promised_eta=now) per spec §4.5, records audit action="order_modified". cancel_order(order, actor, reason) — sets cancellation_reason/cancelled_at; if status==PREPARING (post-cooking): FSM transition→ON_RESALE + creates resale Order copy (order_number+"-RS", resale_of_order_id=orig.id, exclusion_hash=sha256("{phone}:{address_id}")) returns resale; else plain FSM→CANCELLED returns None. Hoisted record_audit + hashlib to module-top imports. tests/ordering/test_modification.py (4) + test_cancellation.py (3) = 7 pass (use autouse restaurant id=1 from tests/ordering/conftest.py — plan-seeded, NOT propagating to new conversation tests). NO migration (all resale/cancellation columns already on Order model). Commit 2ab1f78. ruff clean.
- [2026-06-06 10:16 +04] PHASE 5 FRONTEND Tasks 9-10 DONE (TDD, vitest). T9 LiveOpsScreen (commit on feat/phase-3): KPI strip (7 tiles derived from polled orders), placeholder dispatch-map panel, SLA board two lanes (yellow=5-10min remaining, red=≤5min) via SLAOrderCard, live order feed w/ status-filter pills via LiveOrderRow; polls fetchOrders @4s through usePoll; local SectionBanner on poll error. PLAN-DEFECT FIX: plan test asserts getByText("Ali Hassan") single match, but T8 SLAOrderCard (committed) renders customer_name AND so does LiveOrderRow → order 47 (yellow lane) duplicated → ambiguous match. Resolved by excluding lane-resident order ids from the feed (laneIds Set) — also better UX (no dup of a critical order). Lint fix: usePoll error is `unknown`, `{error && …}` not a valid ReactNode → guarded with `error != null`. T10: CompactTable (generic typed table, empty-state, optional row click), OrdersScreen (fetchOrders on mount, search filter over #id/name/phone, CompactTable rows, no-match empty text), OrderDetailDrawer (SideDrawer + fetchOrder by id, StatusPill+CountdownTimer header, customer/address/rider fields, item lines, total). vitest 50→57 (+2 LiveOps, +2 CompactTable, +3 Orders), tsc --noEmit clean, full suite 57 passed/18 files. SCOPE: frontend/ only — did NOT touch Python/src/apps/tests, no pytest, no other agents' files. Built on committed T1-T8 (ordersApi fixture-fallback w/ fetchOrder, CountdownTimer, KPITile, SLAOrderCard, LiveOrderRow, SideDrawer, StatusPill, SectionBanner — all prop signatures matched plan).
- [2026-06-06 10:15 +04] P3-T6 DONE (TDD): conversation engine ordering states. Created src/app/ordering/service.py (get_or_create_customer, get_last_address, upsert_address, create_draft_order, add_item w/ subtotal recalc, parse_qty_and_text [2x/x2/word], finalize_confirmation draft→pending_confirmation→confirmed + sla_confirmed_at/sla_deadline=+40min/promised_eta). Extended src/app/conversation/engine.py: dispatcher routes menu_sent+collecting_items→_handle_collecting_items (find_dish_matches: DIRECT adds item w/ qty + creates/reuses draft_order_id in conv.state, AMBIGUOUS asks dish number, NO_MATCH polite retry, "what is X"→describer, "done"→address_capture), address_capture/address_text_pending→_handle_address_capture (LOCATION pin→haversine distance_km vs restaurant.lat/lng→calculate_fee, UndeliverableError>10km→sorry msg; text "room, building"→receiver_details), receiver_details→_handle_receiver_details (upsert confirmed address, set order.address_id/distance_km/delivery_fee/total, →order_confirmation + summary), order_confirmation→_handle_order_confirmation (confirm_order btn→finalize_confirmation→order_placed; cancel_order→FSM CANCELLED). WEATHER WIRED (stub agent deferred to me): _send_order_summary calls get_weather_port().is_delay_active()→sets order.weather_delay_disclosed=True + summary note (so weather-caused late delivery skips auto-coupon). PLAN DEVIATIONS (justified): plan used restaurant.latitude/.longitude but model is lat/lng; plan imported calculate_fee not calculate_delivery_fee (matched real signature); plan tests hardcoded restaurant_id=1 → adapted to dynamic `restaurant` fixture per project lesson (never hardcode FK parent PKs); refactored repeated enqueue_message into _send_text/_send_buttons/_set_state helpers. REGRESSION FIX: test_second_message_after_menu_sent_does_not_resend_menu asserted pass-through (now obsolete — menu_sent routes to item-collection); updated to assert menu not RE-sent (1 menu send). tests/conversation/test_engine_ordering.py: 7 new tests. FULL SUITE (excl foreign WIP tests/ordering/test_service.py per controller note): 125 passed. ruff src apps tests CLEAN. No alembic changes (no new columns). Single head still 4ed3145276fb.
- 2026-06-06 10:11 +04 — P5-T7+T8 DONE (frontend/ only, TDD, branch feat/phase-3). T7 orders data layer: src/lib/fixtures/orders.json (3 sample OrderOut rows #47 preparing/#48 assigned/#49 confirmed), src/lib/ordersApi.ts (fetchOrders/fetchOrder — try live /api/v1/orders then FIXTURE-FALLBACK on 404 or non-ApiError network throw [Phase 3 T9 endpoint may not be deployed]; re-raises real ApiError non-404), src/components/CountdownTimer.tsx (1s setInterval tick, remainingMs+slaTier from lib/sla, tierColorVar inline color, urgent <5min size bump + breach pulse class), KPITile.tsx (label/DM-Mono value/±delta arrow in sla-safe|sla-critical). Tests ordersApi 3 (live/404-fallback/network-fallback) + CountdownTimer 3 (MM:SS/critical-color/00:00-freeze) + KPITile 2 (pos/neg delta color) GREEN. Commit (T7). T8 SLA cards: SLAOrderCard.tsx (slaTier→s[tier] lane class [safe has no css class=no border accent, warn/critical/breach styled], id/customer/items-summary/CountdownTimer/StatusPill/rider, onClick role=button) + LiveOrderRow.tsx (grid feed row #id/cust/items-ellipsis/StatusPill/rider/CountdownTimer, onOpen(id) click, isNew slide-in). Tests SLAOrderCard 3 (renders id+cust+countdown / critical<10min / breach>40min) + LiveOrderRow 2 (renders fields / onOpen click) GREEN. Commit (T8). NO plan deviations — files verbatim per plan Tasks 7-8. FULL vitest suite: 50 passed (15 files: prior 37 + ordersApi 3 + CountdownTimer 3 + KPITile 2 + SLAOrderCard 3 + LiveOrderRow 2). tsc --noEmit clean, vite build clean (170kb js). NOT touched: src/ (Python), apps/, tests/, alembic, any backend — only staged my 15 frontend files per-task; pre-existing uncommitted backend changes (apps/simulator/router.py, tests/test_simulator_ordering.py from Wave 5 gate agent) left untouched/unstaged.
- 2026-06-06 10:08 +04 — P5-T5+T6 DONE (frontend/ only, TDD, branch feat/phase-3). T5 primitives: Button (primary/ghost/danger variants, spread props), Spinner (role=status), StatusPill (14 FSM labels + per-status --pill color var, muted fallback for unknown), SectionBanner (warning/error/info/success tones + optional dismiss), SideDrawer (480px right slide-in, scrim-click onClose, null when closed) — each w/ colocated .module.css using var(--token) only. Tests StatusPill.test.tsx (3: label/color-var/unknown-fallback) + SideDrawer.test.tsx (3: open renders/closed hides/scrim onClose) GREEN. Commit (T5). T6 shell+router+login: LoginScreen (phone/password form → lib/auth login → nav("/"), ApiError.detail→SectionBanner error), NavSidebar (7 flat NavLinks, active state via accent-primary-dim, conversations unread badge), AppShell (sidebar + main slot + connectionDown warning banner), App.tsx real router (react-router-dom Routes + Guarded wrapper: !isAuthenticated()→<Navigate to=/login replace>, else AppShell-wrapped screen; catch-all *→"/"), 7 placeholder screen exports (Live Ops/Orders/Menu/Riders/Conversations/Analytics/Settings — replaced in T7-13 except Analytics). Tests LoginScreen.test.tsx (2: submit stores token, 401→error banner) + App.test.tsx (2: unauth redirect shows Sign In, authed shows shell) GREEN. PLAN DEVIATION (justified): plan's App.test asserts getByText("Live Ops") for authed-shell case, but NavSidebar ALSO renders nav label "Live Ops" → getByText threw TestingLibrary multiple-match; renamed LiveOpsScreen placeholder text to "Live Operations Board" (placeholder is throwaway, replaced in T7) so the nav "Live Ops" stays the single stable shell marker — plan test kept verbatim. FULL vitest suite: 37 passed (10 files: prior 27 + StatusPill 3 + SideDrawer 3 + LoginScreen 2 + App 2). tsc --noEmit clean, vite build clean (55 modules). Commits per-task. NOT touched: src/, apps/, tests/, alembic, any Python (Wave 4 owns); only untracked file in tree is Phase 6 planner's doc (left alone).
- 2026-06-06 10:06 +04 — P5-T4 DONE (frontend/ only, TDD). src/lib/sla.ts (SLA_WINDOW_MS=40min; remainingMs(slaStartedAt,now) → full window if null else window-elapsed; slaTier safe/warn<15min/critical<10min/breach≤0; tierColorVar tier→css var [text-primary/sla-warn/sla-critical/sla-breach]; formatCountdown MM:SS clamped at 00:00). Transport abstraction (WS-swappable seam): src/lib/transport/index.ts (Transport<T> interface + Listener/ErrorListener types), transport/pollingTransport.ts (PollingTransport class: immediate tick on first subscribe + setInterval, multi-listener Set, error→onError without stopping loop, auto-clear timer on last unsub). src/lib/usePoll.ts (React hook wrapping PollingTransport, fetcherRef avoids effect re-run, returns {data,error}, default 4000ms). Tests: sla.test.ts 9 (countdown/tiers/format), transport/pollingTransport.test.ts 2 (immediate+interval+unsub stop, error-recovery), usePoll.test.tsx 1 (renders fetched). RED verified → GREEN. FULL vitest suite: 27 passed (6 files: tokens 9, sla 9, polling 2, auth 3, apiClient 3, usePoll 1), tsc --noEmit clean. Commit 3ea25b0. tierColorVar exported-but-unused now (consumed by CountdownTimer in plan T7) — valid, noUnusedLocals doesn't flag exports.
- 2026-06-06 10:05 +04 — P5-T3 DONE (frontend/ only, TDD, branch feat/phase-3). src/lib/types.ts (all backend DTOs: RestaurantOut, TokenOut, RiderOut/RiderStatus, DishOut, DiffOut, MenuOut/MenuWithDiffOut, OrderStatus 14-state union mirroring fsm.py, OrderItemOut, OrderOut, ConversationOut, MessageOut). src/lib/apiClient.ts (typed fetch wrapper: ApiError{status,detail}, bearer injection from localStorage ops_token, JSON+FormData bodies, 204 passthrough, non-2xx→ApiError w/ detail-string coercion; get/post/patch/delete/postForm + TOKEN_KEY export). src/lib/auth.ts (getToken/setToken[+SECURITY httpOnly-migration comment]/logout/isAuthenticated/login POST /api/v1/auth/login {phone,password}→persist access_token). Tests: apiClient.test.ts 3 (bearer present/absent, ApiError 401), auth.test.ts 3 (store/read, logout clears, login persists). RED verified (module-not-found) → GREEN 6/6, tsc --noEmit clean. NOTE: plan-body T3 scope = types/client/auth only; fixture-fallback client deferred to plan Tasks 6/7/12 (fixtures/orders.json etc) where ordersApi/conversationsApi consume it — not part of T3 deliverable. Commit 1b63bac. NOT touched: src/, apps/, tests/, alembic, Python (Wave 4 owns); concurrent uncommitted Python edits in working tree (engine.py, test_engine_ordering.py) left untouched.
- 2026-06-06 10:00 +04 — P5-T1+T2 DONE (frontend/ only, fully isolated from backend). T1 scaffold: created frontend/{package.json,tsconfig.json,tsconfig.node.json,vite.config.ts,index.html,.env.example} + src/{main.tsx,App.tsx (temp stub),vite-env.d.ts,test/setup.ts} + style placeholders; npm install (182 pkgs); npm run lint + build green, dev server boots :5173. PLAN DEVIATION (justified): tsconfig.node.json had both composite:true + noEmit:true → tsc TS6310 ("referenced project may not disable emit"); replaced noEmit with emitDeclarationOnly:true + outDir ./node_modules/.tmp/tsconfig.node (standard Vite-TS project-references fix). Added frontend/node_modules + frontend/dist + .tmp + *.tsbuildinfo to .gitignore. Commit c40e310. T2 design tokens (TDD): wrote src/styles/tokens.test.ts (9 token assertions) → RED → wrote tokens.css (ALL brief §1 vars verbatim: bg/border/text/SLA/accents/status-pills/map + type + 4px spacing scale), fonts.css (Google Fonts DM Mono + IBM Plex Sans @import), base.css (reset+canvas+dark scrollbars+fade-up/slide-in-right/pulse-critical keyframes+reduced-motion) → GREEN 9/9. PLAN DEVIATION (justified): plan's T2 test uses node:fs/node:path/__dirname but package.json omitted @types/node and tsconfig types lacked "node" → lint TS2307/TS2304; added @types/node@^20 devDep + "node" to tsconfig types array. Commit b3afe6e. vitest: 9 passed (1 file). lint clean, build green. GOTCHA: a concurrent writer in this repo reverted my uncommitted T2 working-tree edits once (placeholders + package.json/tsconfig restored mid-run, same symptom as P4-T1 bullet) — re-applied all + committed immediately to lock. NOT touched: src/, apps/, tests/, alembic, pyproject, any Python; no pytest run.
- [2026-06-06 09:48 +04] P3-T9 DONE (TDD): manager-facing ordering HTTP API. Created src/app/ordering/schemas.py (OrderItemOut, OrderOut full field set incl SLA/resale/cancellation, CustomerOut; all ConfigDict(from_attributes=True)) + src/app/ordering/router.py (prefix /api/v1/orders, tags=[orders]): GET /{order_id} (tenant-scoped Order.restaurant_id==current_restaurant.id, 404 else) + GET "" list (optional ?status filter, ?limit=50, order_by created_at desc). Routers call session directly (no ordering service yet — Task 11). Mounted ordering_router in src/app/main.py create_app (import + include_router, minimal additions). Appended 3 tests to tests/ordering/test_service.py: get_order returns 200/json, 404 unknown, list filters by status. PLAN DEVIATION (justified): plan's router.py imported OrderItem/OrderItemOut unused → dropped (ruff F401). Plan test helper _get_test_token() resolved as create_access_token(restaurant_id=1) targeting the autouse restaurant(id=1) seed fixture (tests/ordering/conftest.py) — NOT auth_headers (auth_headers signup also lands id=1 → restaurants_pkey UniqueViolation, the pre-existing zombie-agent failure noted prior bullet; using seeded-restaurant token is the correct plan-intended path). tests/ordering/ 29 passed (5 in test_service incl 2 prior column tests), ruff clean on router/schemas/main/test_service. Ran against restaurant_test2 per DB-isolation directive (other agent owns restaurant_test). NOT touched: alembic, conversation/engine, ordering/service (Task 11).
- [2026-06-06 09:45 +04] P3-T3 DONE (TDD): fuzzy dish matching. Migration e99f4e761d39 (on head 65800d534af2): CREATE EXTENSION pg_trgm + dishes.name_normalized String(256) + backfill (lower+regexp_replace) + GIN ix_dishes_name_normalized_trgm (gin_trgm_ops). Added name_normalized to Dish model (src/app/menu/models.py). Created src/app/ordering/matching.py: normalize_name(), MatchConfidence StrEnum (DIRECT/AMBIGUOUS/NO_MATCH), MatchResult dataclass, find_dish_matches() — bare-int→exact dish_number lookup; else pg_trgm similarity() on name_normalized within active menu (0.3 SQL floor, top sim ≥ _SINGLE_THRESHOLD, gap>0.15→DIRECT else AMBIGUOUS top-3). tests/ordering/test_matching.py 5 pass. PLAN DEVIATION (justified): lowered _SINGLE_THRESHOLD 0.6→0.5 — pg_trgm 1.6 / PG16.4 scores plan's intended-DIRECT typo similarity('chicken biryani','chikn biryani')=0.579 (<0.6 would wrongly NO_MATCH); NO_MATCH query scores 0.0 (filtered by 0.3 floor) so still correct. Also dropped unused pytest+MatchResult imports from test file (plan had them, ruff F401). tests/conftest.py engine fixture: added CREATE EXTENSION IF NOT EXISTS pg_trgm before create_all (create_all can't make extensions; similarity() needs it). Commit 7a76a1e. FULL SUITE: 121 passed + 2 FAILED (test_get_order_api_returns_order, test_list_orders_api_filters_by_status) — PRE-EXISTING & FOREIGN: 75 uncommitted lines in tests/ordering/test_service.py from another agent (stash 'zombie-agent-damage'); they use autouse restaurant(id=1) + auth_headers signup which also lands id=1 → restaurants_pkey UniqueViolation. NOT my scope, left uncommitted/untouched. ruff src apps tests clean.
- [2026-06-06 09:42] PHASE 5 PLAN WRITTEN: docs/superpowers/plans/2026-06-06-phase-5-dashboard.md (4554 lines, 15 TDD tasks). React manager dashboard — Vite+React18+TS, no UI framework, hand-rolled "Tactical Operations Dark" design system (tokens.css copied verbatim from docs/design/dashboard-design-brief.md §1; DM Mono + IBM Plex Sans). Tasks: (1) scaffold+vitest, (2) design tokens+base css, (3) typed fetch apiClient+auth(localStorage)+DTO types, (4) SLA math (40-min window, safe/warn/critical/breach tiers) + Transport interface/PollingTransport(4s)/usePoll — WS-swappable seam, (5) primitives Button/Spinner/StatusPill(14 FSM states)/SectionBanner/SideDrawer, (6) AppShell+NavSidebar+router AuthGuard+LoginScreen, (7) ordersApi w/ FIXTURE-FALLBACK (orders endpoint = Phase 3 T9, may not exist → 404 falls back to fixtures/orders.json so buildable/testable now)+CountdownTimer+KPITile, (8) SLAOrderCard(lane/breach)+LiveOrderRow, (9) LiveOpsScreen (KPI strip+map placeholder+yellow/red SLA lanes+filterable feed), (10) CompactTable+OrdersScreen(search)+OrderDetailDrawer, (11) MenuManager upload→DiffPanel→activate + DishCard availability toggle, (12) RidersScreen+RiderCard(stale-location hook), (13) Conversations list/MessageBubble/manual takeover (fixture-fallback), (14) Settings(tabbed batching editor)+Analytics placeholder, (15) Playwright smoke (login→live ops, route-stubbed, no live backend)+full-suite gate. SCOPE: Marketing/Predictions/Audit screens + real dispatch map DEFERRED (labeled placeholders). Every task TDD (failing test→impl→pass→commit), real code no placeholders, exact file paths, self-contained for zero-context implementer. NOT committed (controller commits).
- [2026-06-06 09:40] P2-U5 verified complete: conversation service + engine were already implemented/committed (2fc83fb service: get_or_create_conversation/record_message/set_manual_takeover; d252a64 engine: handle_inbound greeting state → renders active menu (available dishes, category-grouped, no price-less leak), enqueues outbox, advances state to menu_sent w/ audit, manual_takeover short-circuit). THIS SESSION: repaired zombie-agent-damaged conversation tests (commit bb8b663 had introduced an autouse _seed_restaurant fixture forcing Restaurant(id=1) — explicit PK collided across the session-scoped restaurant_test DB, contaminating the full run with 70 DBAPIErrors). Converged tests/conversation/{conftest,test_service,test_engine}.py back to the clean per-test `restaurant` fixture (dynamic restaurant.id, flush-not-commit, savepoint-rolled-back). Result: conversation 8 passed; FULL SUITE 115 passed (was 45 passed + 70 errors), ruff clean on src/app/conversation + tests/conversation. LESSON: never seed FK parents with hardcoded PKs under a shared session-scoped DB — use a fixture-returned row id. NOTE: branch is feat/phase-3 (phase-2 conversation work already merged); only unrelated tests/llm/test_llm_ordering_ports.py remains modified in tree.
- [2026-06-06 09:37] P3-T1+T2 DONE (TDD): ordering bounded context created. T1 customers + customer_addresses tables (Customer w/ usual_order_times/tags JSONB, total_spend Numeric(10,2); CustomerAddress lat/lng floats, confirmed/last_used_at) → migration 61767852ea41 w/ trg_customers/customer_addresses_updated_at BEFORE UPDATE triggers; registered app.ordering.models in alembic/env.py + tests/conftest.py. T2 orders + order_items tables (Order: FSM status string, Numeric(8,2) money, SLA/resale/coupon/exclusion_hash fields, self-ref resale_of_order_id FK; OrderItem dish snapshot fields) + fsm.py (OrderStatus StrEnum 14 spec §3 states, OrderFSM.TRANSITIONS explicit adjacency map, validate()→IllegalTransitionError, async transition() applies+audits via record_audit action="order_status_transition", caller-commits) → migration 65800d534af2 w/ trg_orders/order_items triggers. Added tests/ordering/conftest.py autouse restaurant(id=1) fixture (plan tests hardcode restaurant_id=1; rollback doesn't reset seq so id pinned). Commits 0cd971f, df074af. FULL SUITE: 115 passed (ordering 21). ruff clean on src/app/ordering + tests/ordering. NOTE: pre-existing ruff fail in tests/llm/test_llm_ordering_ports.py (b4bf3e3, parallel LLM-ports agent — F401 x3 + E741 ambiguous 'l') left untouched (out of T1/T2 scope, their committed file). GOTCHA: stale restaurant_test schema caused drop_all DependentObjectsStillExistError — reset via DROP SCHEMA public CASCADE before suite went green; test DB uses create_all (no migration triggers/set_updated_at fn there, fine — updated_at also has client onupdate).
- [2026-06-06 09:35] PHASE 2 FINAL REVIEW: READY TO MERGE (83 passed, ruff clean). Sound: raw-body sig verify, commit-before-dispatch (no worker race), tenancy scoped everywhere, replay-safe idempotency (unique keys + worker early-return). DEBT QUEUE → Phase 3: (1) failed outbox rows never retried — need Celery beat sweeper w/ backoff BEFORE prod traffic; (2) dispatch-query race = benign double-dispatch, fix w/ row claim UPDATE...RETURNING later; (3) webhook_events.processed_at String not timestamp; (4) post-menu_sent messages silently dropped (Phase 3 owns ordering states). Merge deferred — Phase 4 planner mid-write on plan file.
- 2026-06-06 09:33 +04 — Phase 3 Task 5 (LLM ordering ports): port.py +DescriberPort/IntentClassifierPort/ArbiterPort Protocols; fake.py +FakeDescriber (regex price-strip, 80-char trunc)/FakeIntentClassifier (rule-based)/FakeArbiter (first-candidate); claude.py +ClaudeDescriber/ClaudeIntentClassifier/ClaudeArbiter (haiku, _get_anthropic_client); factory.py +get_describer/get_intent_classifier/get_arbiter + cached _get_anthropic_client (sync Anthropic). Minor plan deviation for lint: moved `import re as _re` to top + dropped dead `import anthropic`. tests/llm/test_llm_ordering_ports.py: 7 new tests pass (16 total llm), ruff src clean.
- 2026-06-06 09:32 +0400 — Phase 3 Task 10 (weather port stub ONLY): added src/app/weather/{__init__,port,fake,factory}.py — WeatherPort Protocol (is_delay_active()->bool), FakeWeatherPort(delay_active) stub, get_weather_port() factory returning FakeWeatherPort(False). tests/ordering/test_weather_stub.py: 3 pure-python tests pass, ruff clean. DEFERRED (engine integration owned by another agent): Step 5 _handle_order_confirmation weather check + order.weather_delay_disclosed wiring in conversation/engine.py — NOT touched.
- 2026-06-06 09:31 +04 — Phase 3 Task 4: added src/app/geo/haversine.py (distance_km) + src/app/ordering/fees.py (calculate_fee w/ configurable tiers ≤3km AED0/≤5km AED5/≤10km AED10, UndeliverableError >10km); 12 pure-python tests pass, ruff clean.
- [2026-06-06 09:25] P2-U7 done + PHASE 2 GATE PASSED: simulator router mounted (mock only), full pipeline browser→engine→outbox→MockProvider. Tests 83 passed (unit/integration/regression), ruff clean (white-box), boot smoke + E2E simulator roundtrip + security suite (HMAC/handshake 403) + perf sanity OK.
- [2026-06-06 09:25] Phase 4 logistics plan COMPLETED (Tasks 5-17 appended: geo fees, rider scoring, batching, dispatch engine, delivery FSM, rider location, rider button flow, COD, coupons, SLA monitor, live tracking, Celery wiring, dispatch router + smoke).
- [2026-06-06 09:24] P2-U6 done: webhook router (GET handshake, signed POST pipeline with cloud-mode HMAC verify, per-message idempotency gate via webhook_events, restaurant-phone resolution, conversation engine commit, then dispatch pending OutboxMessage rows via deliver_outbox_message.apply_async queue=outbox) mounted in main.py. WIRING FIX: router imports apps.workers.celery_app so @shared_task binds to redis broker (else falls back to amqp default → connection refused). Tasks 11+12 committed (Task11 first commit, 93c64cf Task12). FULL SUITE: 80 passed, ruff clean.
- [2026-06-06 09:22] GAP INVESTIGATION COMPLETE: (1) restaurant_phone not +-normalized in normalizer — fixed de90ac5 (Meta sends display_phone_number without +); (2) messages.ts Integer 2038 overflow — BigInteger migration 352c3723491c (86099f3); (3) U4/U5 agents DIED at spend limit mid-work — finished by controller: worker tests rebound to db_session.bind savepoint factory (cross-connection invisibility bug), conversation conftest+fixture FK fix, engine.py+test_engine.py written. Commits a0b5510, 2fc83fb, d252a64. FULL SUITE: 75 passed, ruff clean. (4) Phase 4 plan STALE at 744 lines/4 tasks (planner died) — needs re-dispatch. NEXT: P2-U6 webhook router.
- [2026-06-06 06:10] Phase 4 plan drafted → docs/superpowers/plans/2026-06-06-phase-4-logistics.md (17 TDD tasks, self-contained, builds on Phase 3). Scope: geo/ port (GeoPort + FakeGeoProvider haversine/25kmh static, GoogleMaps stub w/ graceful degradation + is_estimate flag) and canonical delivery-fee tiers (≤3km free/≤5km AED5/≤10km AED10/>10km reject); dispatch/ (Batch/BatchOrder/RiderLocation/Assignment models, rider scoring=distance+workload+on-time% composite persisted to assignments.algorithm_score, proximity batching max3/10-min window/+10min buffer, priority-first single-rider, no-riders→manager outbox alert, Redis lock per restaurant); delivery FSM assigned→picked_up→arriving→delivered (spec §3 strings — used "arriving" not "en_route" to match Phase 3 OrderFSM); sla/ (SlaEvent + Celery-beat monitor yellow_30/red_35/breach_40, proactive customer+manager msgs, auto-coupon EXCEPT weather_delay_disclosed); coupons/ (single-use, 30-day expiry, issue+redeem); cod/ (CodCollection idempotent per order + RiderShiftReconciliation expected/collected/variance). New migrations: order.rider_id FK, rider.performance JSONB, all 8 tables w/ trg_<table>_updated_at triggers + restaurant_id tenancy. Celery: dispatch + sla_monitor queues, dispatch_for_restaurant on-demand task, sla_monitor_tick beat, POST /api/v1/dispatch/trigger manager endpoint. Rider WhatsApp flow: counterpart="rider" routing, location pings → rider_locations + Redis GEO, "Orders Picked"/"Delivered/Collect money" buttons drive FSM (button-only flow integrity). NOTE for implementer: FSM uses ARRIVING (Phase 3) where Phase-4 scope brief said en_route — kept consistent w/ existing OrderFSM to avoid illegal-transition breakage.
- [2026-06-06 06:00] GAP SWEEP: P2-U3 agent reported commits but never committed — restored bc3bc89 (normalizer) + 956c03f (4 tables). Phase 3 plan + CLAUDE.md committed 9638fb5. Verified: alembic single head ba337a5ead4a, 4 tables live in dev DB, ruff clean. Quality review P2-U1/U2 APPROVED — 1 minor fixed (153c021 NotImplementedError unknown msg type). U4+U5 agents running parallel.
- [2026-06-06 05:55] Phase 2 Tasks 6+7 done (62 passed, ruff clean). Task 6: src/app/webhook/__init__.py + normalizer.py (parse_cloud_payload → InboundMessage list; handles text/button_reply/list_reply/location/image/unknown; +prefix normalizer; status updates → empty list). 4 new tests in tests/whatsapp/test_normalizer.py, all passing. Task 7: WebhookEvent (webhook_events), OutboxMessage (outbox_messages), Conversation+Message (conversations+messages) models created under src/app/webhook/models.py, src/app/outbox/models.py, src/app/conversation/models.py; all registered in alembic/env.py + tests/conftest.py; migration ba337a5ead4a generated+applied with BEFORE UPDATE triggers trg_*_updated_at for all 4 tables. db.py gains async_session_factory = get_session_factory() alias for Celery workers.
- [2026-06-06 05:55] PHASE 0+1 MERGED TO MAIN (47 tests on main). Branch feat/phase-2 created. P2-U1 done (63d3bca, 1f6b455, 9c7e6a7): diff orchestration → menu/service.upload_with_diff (router thin again), AvailabilityIn → schemas, Dish.menu lazy="raise_on_sql", WA settings (whatsapp_provider/wa_verify_token/wa_access_token/wa_phone_number_id/wa_app_secret), whatsapp/port.py (StrEnums + dataclasses + Protocol). 51 passed. P2-U2 (providers) dispatched.
- [2026-06-06 05:55] Phase 3 plan drafter v1 DIED at 32k output cap (file never written) — re-dispatched with chunked-write discipline (incremental Writes + ≤30-line report). LESSON for all doc agents: never single-shot huge files.
- [2026-06-06 05:48] FINAL REVIEW PHASE 0+1: READY. Units 7+8 spec ✅ all compliant. 47 passed, ruff clean, alembic single head 6695a9376ea3, all biz-rule spot checks pass (activation gate, fee tiers, instant availability, supersede). PHASE 2 QUEUE (do first→last): (1) extract diff orchestration from upload_menu router → menu/service.py, (2) AvailabilityIn → schemas.py, (3) "max 3 lines never price" enforcement belongs in conversation rendering, (4) lazy="raise_on_sql" on Dish.menu, (5) decide: persist uploaded menu file bytes to upload_dir or keep metadata-only (currently metadata-only — re-extraction needs re-upload), (6) CI boot-smoke tests, (7) diff_menus null-number key collision if draft-vs-draft ever needed.
- [2026-06-06 05:40] PHASE 0+1 PLAN COMPLETE (T1-T19). Unit 8: diff.py (number+name match, price changes/added/removed/conflicts) surfaced as diff_vs_active in upload response; availability toggle endpoint. Unit 9: riders CRUD + settings patch + identity/service.py layer (M6 debt paid — signup refactored through service, routers thin). Verified by controller: 42→47 passed, ruff clean. Smokes: /health ok, signup 201 w/ default tiers, celery boots vs redis. Commits through bbef9c6+. Pending: Units 7+8 spec review, FINAL whole-phase review (running, owns pytest), Phase 3+4 plan drafts.
- [2026-06-06 05:40] Producer/consumer map current: Settings → db(lazy engine)/auth/llm factory/celery; conftest overrides get_session + get_menu_extractor; menu.service consumes llm port (ValueError→422 RuntimeError→502 at router); audit consumed by identity.service + menu router/service; activation gate = sole enforcement point of dish-number-mandatory rule until ordering phase reads active menu.
- [2026-06-06 05:28] Unit 7 done (31 passed): dish add/patch/delete (409 dup number, audit before/after) + activation (completeness gate 422, supersede previous active). Savepoint adaptations: delete_dish expire_all() after commit (stale identity map), activate-block test uses flush not commit.
- [2026-06-06 05:28] Unit 4.5 done: timing-oracle dummy-hash login, missing-sub token → 401 (was 500), deepcopy settings default, audit +lat/lng, LoginIn/lat/lng validation, 4 new auth tests (13 identity total). M5: NO dup phone index (SQLAlchemy merged unique+index → single). conftest now setdefault for APP_DATABASE_URL → parallel pytest via restaurant_test2 ENABLED (pattern documented here for future agents).
- [2026-06-06 05:20] Unit 6 spec ✅. Identity quality review: fix-first → Unit 4.5 queued (I1 missing-sub JWT → KeyError → 500 — widen except; I2 login timing oracle ~50ms — module-level dummy argon2 verify; minors: deepcopy settings default, audit lat/lng, LoginIn lengths, lat/lng range validation, dup phone index check, tests for expired/wrong-scheme/missing-sub tokens). DEFERRED: identity/service.py split → Unit 9 (riders); login rate limiting → hardening phase. JWT aud/iss claim needed when manager_users/rider principals arrive — note for Phase 4+.
- [2026-06-06 05:20] Scheduling discipline: only ONE pytest owner at a time (shared restaurant_test DB, session-scoped drop/create races) — reviewers run read-only + ruff with .venv/bin path.
- [2026-06-06 05:12] Unit 6 done: menu/dish models + migration 6695a9376ea3 (clean — include_object filter proven; updated_at triggers per convention), upload endpoint POST /api/v1/menus (multipart → extractor → drafts; ValueError→422, RuntimeError→502), GET /menus/{id} tenant-scoped, conftest gains FakeExtractor override + auth_headers. 24 passed. Savepoint note: service-level session.commit() lands on savepoint under join_transaction_mode="create_savepoint" — works.
- [2026-06-06 05:12] Identity spec review ✅ (T7-T9 line-by-line compliant, flush→audit→commit ordering verified). Identity quality review + Unit 6 spec review + Unit 7 implementer (edit/activate) + Phase 3/4 plan drafters running. Dashboard design brief landed: docs/design/dashboard-design-brief.md (dark tactical ops terminal, DM Mono + IBM Plex Sans, SLA color threading, 9 screens).
- [2026-06-06 05:05] Unit 2.5 hardening landed (366bb0f): db.py lazy get_engine()/get_session_factory() (lru_cache; no import-time engine), updated_at BEFORE UPDATE triggers migration (audit_log/restaurants/riders) + composite audit index (restaurant_id, entity, entity_id), conftest rollback isolation (session-scoped engine + savepoint sessions; pyproject: asyncio_default_fixture_loop_scope AND asyncio_default_test_loop_scope = "session" — both needed else cross-loop asyncpg failures), compose pgdata volume + redis healthcheck (test DB recreated post-volume-switch), jwt dev default 36 chars, ValidationError→RuntimeError llm test. RULE for future tests: session-scoped loop; new TimestampMixin tables need trg_<table>_updated_at trigger in migration.
- [2026-06-06 05:05] Unit 4b merged (a9b6ba7): /api/v1/auth/signup, /auth/login, /me + current_restaurant tenant dep (HTTPBearer JWT). FULL SUITE: 21 passed, ruff clean. Phase 0+1 plan T1-T11 complete; remaining T12-T19 (menu chain + riders/settings + final gate). Docs committed 5d6db72 (phase-2 plan, research, this log).
- [2026-06-06 05:00] Unit 5 re-review: ✅ APPROVED. All 6 guards verified, cache hygiene in factory test confirmed. Minor follow-up → folded into Unit 2.5: add test for ValidationError→RuntimeError path (malformed dish, e.g. name as int).
- [2026-06-06 04:58] Worktree A merged + integration commit 4951706: alembic env.py now has include_object filter (reflected && compare_to is None → skip) — KILLS recurring PostGIS/Tiger drop-table pollution in autogenerate permanently. restaurants_riders migration regenerated clean + applied. identity models registered in alembic env + conftest. FULL SUITE: 15 passed, ruff clean. Worktrees removed, branches deleted. State: T1-T8 + T10-T11 done; remaining plan T9 (signup/login routes), T12-T19 + Unit 2.5 hardening.
- [2026-06-06 04:58] Research landed: docs/research/whatsapp-cloud-api-notes.md (webhook shapes, 3-button/10-row limits, media URL 5-min expiry, lat/lng strings-out/floats-in, template name 30-day reuse blackout, 100 templates/hr/WABA) + docs/research/meta-template-compliance.md (UAE 9am-6pm marketing window, per-user 2-marketing-msgs/day Meta cap = top delivery risk, datestamped template names mandatory). Phase 2 plan persisted: docs/superpowers/plans/2026-06-06-phase-2-whatsapp-core.md (NOT yet committed — commit with next batch).
- [2026-06-06 04:58] Unit 2 quality review: APPROVED w/ queued fixes (Unit 2.5 task): import-time engine fragile (lazy getter + get_settings.cache_clear pattern), updated_at onupdate is client-side (→ server_onupdate/trigger), drop_all/create_all per test won't scale (→ rollback isolation before test count grows), composite audit index (restaurant_id, entity, entity_id), redis healthcheck, named pgdata volume (currently `docker compose down` wipes dev data — conscious choice pending), record_audit needs caller-must-commit docstring. Plan doc test snippet stale vs SecretStr (impl better than plan — keep impl).
- [2026-06-06 04:55] Unit 4a merged (c5f06cd): identity models (Restaurant w/ JSONB settings defaults incl fee tiers, Rider w/ status enum-string) + argon2/JWT auth service. Smoke: 3 identity tests pass post-merge. Concerns logged: dev jwt_secret default <32 bytes (HS256 warning — fix in Unit 4b: lengthen default), passlib crypt deprecation (upstream; revisit in hardening phase — candidate swap to argon2-cffi direct). Migration for restaurants/riders NOT yet generated — waits for worktree A's alembic.
- [2026-06-06 04:52] Unit 5 hardening landed (e6c8b8d): ClaudeExtractor max_tokens 16384 + truncation guard, _IMAGE_MIMES allowlist, empty-files ValueError, dishes/ValidationError → RuntimeError; factory lru_cache singleton + unknown-provider ValueError. llm suite now 8 tests. Re-review agent running. Error contract for consumers: ValueError = caller/input fault (HTTP 422), RuntimeError = extraction/model fault (HTTP 502 + manager alert).
- [2026-06-06 04:49] Unit 5 quality review = fix-first. Fix agent running (main tree, src/app/llm only): max_tokens 16384 + stop_reason guard, image mime allowlist, empty-files guard, dishes-key + ValidationError guards → RuntimeError, factory @lru_cache + unknown-provider ValueError. CONSUMER IMPACT: menu upload endpoint (Unit 6) must map ValueError→422, RuntimeError→502/manager alert.
- [2026-06-06 04:49] Alembic caveat from postgis image: autogenerate may try dropping spatial_ref_sys etc — worktree A instructed to strip PostGIS artifacts from migration.
- [2026-06-06 04:47] PARALLEL implementation started per user request. Worktrees: ../rws-wt-unit3 (branch wt/unit-3: main.py+/health, conftest client fixture, alembic init+audit_log migration, celery skeleton) and ../rws-wt-unit4a (branch wt/unit-4a: identity models + argon2/JWT auth svc, NO migration/conftest — merged later by controller to avoid conflicts). Background research agents: Phase 2 plan draft, WhatsApp Cloud API notes, Meta template compliance (→ docs/research/).
- [2026-06-06 04:45] Unit 5 (llm port/fake/claude) DONE + spec ✅. Unit 2 (docker-compose postgis 5433 + redis 6380, db.py Base/TimestampMixin/engine/get_session, conftest engine+db_session fixtures, audit module) DONE + spec ✅. 6 tests green.
- [2026-06-06 04:41] Docker resolved WITHOUT Docker Desktop: colima VM started (engine 27.4.0) + brew docker-compose v5.1.4 linked as CLI plugin (~/.docker/cli-plugins). `docker compose` works.
- [2026-06-06 04:37] Unit 1 review cycle complete (spec ✅, quality fix-first → fixed → re-review ✅). Fixes in ae5f943: *.egg-info/ gitignored + de-indexed, jwt_secret & anthropic_api_key now pydantic SecretStr (RIPPLE: auth.py must use settings.jwt_secret.get_secret_value(); ClaudeExtractor must use settings.anthropic_api_key.get_secret_value()), APP_CLAUDE_MODEL documented in .env.example. Smoke: pytest 2 passed, ruff clean.
- [2026-06-06 04:37] BLOCKER: Docker Desktop not running — Unit 2 (postgres:5433 + redis:6380) waiting on it.
- [2026-06-06 04:31] Unit 1 (plan T1-T2) done by subagent: pyproject.toml (FastAPI/SQLAlchemy/Celery/anthropic/passlib/pyjwt + dev pytest/httpx/ruff), .gitignore, .env.example (APP_ prefix), src/app + apps/workers + tests packages, .venv installed (py3.12.0), src/app/config.py Settings (pydantic-settings, env_prefix APP_, cached get_settings). Tests: 2 passed. Commits: "chore: scaffold project skeleton and dependencies", "feat: typed settings with APP_ env prefix".
- [2026-06-06 04:31] Producer/source/consumer map so far: Settings (producer of config) ← consumed by future db.py engine, celery_app broker, llm factory, auth JWT. No runtime wiring yet beyond config.
- [2026-06-06 04:28] CLAUDE.md added — project guidance: module layout (models/schemas/service/router per bounded context), ports for LLM/WhatsApp/geo, tenancy via restaurant_id + JWT, audit-everything, alembic+conftest model registration rule.
- [2026-06-06 04:18] Implementation plan committed at docs/superpowers/plans/2026-06-06-phase-0-1-foundation.md — 19 TDD tasks for Phase 0 (scaffold) + Phase 1 (identity + AI menu).
- [2026-06-06 04:10] git repo initialized on main; design spec committed at docs/superpowers/specs/2026-06-06-whatsapp-restaurant-platform-design.md — single source of truth for business rules (40-min SLA, COD, batching, riders-are-employees, coupon rules, resale exclusions).
- [2026-06-06 10:18 +04] DEPLOY DONE (deploy-artifacts agent, branch feat/phase-3, scope = Dockerfile/compose/scripts/docs ONLY — did NOT touch src/apps/tests/frontend/alembic/CI). Added: Dockerfile (multi-stage python:3.12-slim — builder pip-wheels w/ build-essential for argon2/asyncpg, runtime non-root user `app` uid10001, installs runtime deps from wheels via --no-index then `pip install --no-deps -e .`, NO dev extras, curl HEALTHCHECK /health, uvicorn CMD --workers ${APP_WORKERS}); Dockerfile.worker (same base, celery worker CMD queues=outbox,default concurrency=${APP_CONCURRENCY}, celery inspect ping healthcheck, beat reuses image via compose command override); docker-compose.prod.yml (services db[postgis:16-3.4 named vol pgdata, healthcheck]/redis[7, --appendonly yes, redisdata vol, healthcheck]/migrate[one-shot alembic upgrade head, depends_on db healthy]/api[Dockerfile, port ${API_PORT:-8000}, env_file .env, depends_on migrate service_completed_successfully + db/redis healthy, healthcheck]/worker/beat[single-instance]; YAML anchor x-app-env overrides APP_DATABASE_URL→db:5432 + APP_REDIS_URL→redis:6379 so .env need not carry in-network URLs; POSTGRES_PASSWORD required[fail-fast :?]; restart unless-stopped; mem_limit per svc; db/redis NOT host-published [expose only]; no host-mounted code). .dockerignore (.venv/__pycache__/tests/docs/frontend/.git/.env*/egg-info/var). docs/deployment.md (runbook: build, full APP_* env-var table from src/app/config.py w/ secrets flagged [JWT_SECRET/ANTHROPIC/WA_*/GOOGLE_MAPS/POSTGRES_PASSWORD], first-boot migrate, Meta webhook GET handshake registration @ /webhooks/whatsapp echoing hub.challenge, scaling [api/worker horizontal-safe stateless, beat single-instance, MockProvider=dev-only→prod must APP_WHATSAPP_PROVIDER=cloud], pg_dump backup of pgdata, stdout logs). scripts/prod-smoke.sh (chmod +x: compose config-check→up --build→poll api Health=healthy w/ migrate-failure short-circuit→assert /health "status":"ok"→webhook handshake echoes challenge→bad-token=403→down -v; throwaway .env from .env.example w/ known verify token, trap cleanup). VALIDATION: `docker compose -f docker-compose.prod.yml config -q` exit 0 (colima docker 27.x) — all 6 services/conditions/mem_limits/in-network URLs resolve; did NOT build images per directive (config-validate only); bash -n smoke clean. GOTCHA: compose env_file requires .env to EXIST for `config` to parse — validated by copying .env.example→.env then rm (never committed, gitignored). Foreign untracked left untouched: README.md + docs/.../phase-7-hardening.md (other agents). Commit next.
